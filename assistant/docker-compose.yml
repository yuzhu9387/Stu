services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: guoyuzhu
      POSTGRES_PASSWORD: guoyuzhu
      POSTGRES_DB: my_assistant
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
